import type { UserOptions } from "./types/UserOptions.js";

export const defaultUserOptions: UserOptions = {
  options: {
    operationList: {
      filters: {
        account: "",
      },
    },
  },
};
