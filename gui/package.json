{
  "dependencies": {
    "@nuxtjs/eslint-config-typescript": "^12.0.0",
    "@pinia/nuxt": "^0.4.4",
    "bootstrap": "^5.2.2",
    "prettier": "^2.7.1"
  },
  "devDependencies": {
    "nuxt": "3.0.0",
    "tsc-files": "^1.1.3",
    "vue-tsc": "^1.0.9"
  },
  "scripts": {
    "build": "nuxt build",
    "check-types": "tsc --noEmit --project ./tsconfig.json && nuxi typecheck .",
    "dev": "nuxt dev",
    "format": "prettier --write .",
    "lint": "eslint --cache --fix .",
    "lint-staged": "lint-staged",
    "generate": "nuxt generate",
    "postinstall": "nuxt prepare",
    "preview": "nuxt preview"
  }
}
