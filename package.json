{
  "dependencies": {
    "eslint-plugin-simple-import-sort": "^10.0.0",
    "husky": "^8.0.0",
    "lint-staged": "^13.0.4"
  },
  "name": "capim",
  "scripts": {
    "lint-staged": "lint-staged",
    "prepare": "husky install",
    "s": "npm run s --workspace main",
    "start": "npm run dev --workspace main",
    "x": "npm run x --workspace main"
  },
  "workspaces": [
    "main",
    "tests"
  ]
}
