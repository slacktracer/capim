{
  "dependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.0.4"
  },
  "name": "capim",
  "scripts": {
    "lint-staged": "lint-staged",
    "prepare": "husky install",
    "start": "npm run dev --workspace main"
  },
  "workspaces": [
    "main",
    "tests"
  ]
}
